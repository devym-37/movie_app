import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />/*component가 data를 보여준다. 
    component는 HTML을 반환하는 함수
    call - JSX, react의 특별한 개념*/, document.getElementById('root'));


